%h3= t('.title')
- @items.each do |item|
  .card.mt-2
    .card-body
      %p #{item.name} : #{item.price}
      = link_to t('.edit'), edit_item_path(item)
      = link_to t('.delete'), item_path(item), method: :delete
= link_to t('.add_item'), new_item_path, class: 'btn btn-outline-success mt-2'
