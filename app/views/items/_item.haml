- items.each do |item|
  .col-md-3
    .card.mt-3
      .card-body
        %p #{item.name} (#{item.meal_type})
        = link_to t('.edit'), edit_item_path(item)
        - if @facade.allowed_delete?(item)
          = link_to t('.delete'), item_path(item), method: :delete
