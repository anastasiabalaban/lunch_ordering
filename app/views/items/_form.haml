= simple_form_for @item do |f|
  = f.input :name
  = f.input :meal_type, as: :radio_buttons, collection: Item.meal_types.keys
  = f.button :submit, class: 'btn btn-outline-success mb-2'
= link_to t('.back'), items_path, class: 'btn btn-light mt-2'
