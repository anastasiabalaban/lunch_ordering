.nested-fields
  = f.input :name
  = f.input :meal_type, as: :radio_buttons, collection: %w[first main drink]
  = link_to_remove_association t('.remove_item'), f, class: 'btn btn-sm btn-outline-danger mb-2'
